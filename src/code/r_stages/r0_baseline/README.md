# R0 - Baseline

- **Purpose**: Establishes a baseline student count projection using persistence delta assumptions.
- **Input**: Model views (`r0_baseline_1yr.csv`).
- **Output**: Performance metrics long format.
- **Feature Group**: None (ML training bypassed).
- **Model Group**: PersistenceDelta0 (zero-change prediction).
- **Relationship to Other Stages**: Acts as a non-learning benchmark against which R1-R6 models are measured.


